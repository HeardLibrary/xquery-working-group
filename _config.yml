# Site settings
title: XQuery Working Group
description: 'A working group of the Vanderbilt Jean & Alexander Heard Libraries'
url: 'https://heardlibrary.github.io/xquery-working-group/'
baseurl: '/'
# google_analytics: 'UA-XXXXXX-X'
# disqus_shortname: 'your-disqus-name'

author:
  name: 'Jean and Alexander Heard Libraries'
  email: clifford.anderson@vanderbilt.edu
  twitter_username: VandyLibraries
  github_username:  heardlibrary

defaults:
  -
    scope:
      path: ''
      type: 'posts'
    values:
        layout: 'post'

    scope:
      path: ''
      type: 'pages'
    values:
        layout: 'page'

# Build settings
destination: _site
paginate: 10
permalink: /:year/:title/
markdown: kramdown
highlighter: rouge

sass:
    sass_dir: _sass
    style: compressed

kramdown:
  # use Github Flavored Markdown
  input: GFM
  # do not replace newlines by <br>s
  hard_wrap: false

gems: ['jekyll-paginate']
exclude: ['README.md', 'Gemfile', 'Gemfile.lock', 'screenshot.png']
include: '_pages'
